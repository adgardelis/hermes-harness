# HERMES HARNESS — recorded demonstration

`demo-2026-09-07.typescript` is a `script(1)` terminal recording of the public
benchmark suite running against `tools/tool_result_storage.py` @
`49b044a322b5a87e4f45fb18e73d09d0b204165e` on macOS (Apple M5 Max), ending with
`CHECK PASSED` (15/15 cases, exact-byte recovery fidelity 100%).

Replay it:

```sh
cat docs/demo/demo-2026-09-07.typescript
```

Reproduce the run yourself with the commands in `benchmarks/README.md`.

Note: a screen-video capture was attempted first but macOS screen-recording
permission (TCC) was not granted to the recording shell, so the demonstration
is published as a terminal recording instead.
